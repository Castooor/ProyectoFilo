services:
  - type: web
    name: imagen-o-contexto-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir api app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
